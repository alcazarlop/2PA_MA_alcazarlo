
// #include "sprite.h"

// Sprite::Sprite(Texture* tex, 
// 							 int32_t origin_x, int32_t origin_y, 
// 							 int32_t width, int32_t height) {

// 	if((tex != nullptr) && (origin_x >= 0) && (origin_y >= 0) &&
// 		 (width >= 0) && (height >= 0)){
// 		sprite_ = tex->getSprite();
// 	} else {
// 		sprite_ = nullptr;
// 	}

// }

// Sprite::Sprite(const Sprite& copy){

// }

// Sprite::~Sprite(){
// 	// if(sprite_ != nullptr)
// 		// SDL_DestroyTexture();
// }

// void Sprite::size(int32_t* width, int32_t* height) const {
// 	// if((sprite_ != nullptr) && (width >= 0) && (height >= 0)){
// 	// 	width = algo
// 	// 	height = algo
// 	// }
// }

// void Sprite::getPixel(int32_t x, int32_t y, uint8_t color[5]) const {

// }

// void Sprite::draw(){
// 	if(enable_){
// 		if(sprite_ != nullptr){
			
// 		}
// 	}
// }